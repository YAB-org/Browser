export function parse(content) {
  let tree = {};
  content = content.replaceAll(/<!--([^¬]|.)*?-->/g, '');
  let stack = '';
  for (let i = 0; i<content.length; i++) {
    content[i]
  }
  return tree;
}